<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cwww.user.mapper.UserMapper">

    <insert id="insertOAuth" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (email, nickname, provider, provider_id, role, status, created_at, updated_at)
        VALUES (#{email}, #{nickname}, #{provider}, #{providerId}, #{role}, #{status}, NOW(), NOW())
    </insert>

    <select id="findByProviderAndProviderId" resultType="com.cwww.user.domain.User">
        SELECT * FROM users
        WHERE provider = #{provider} AND provider_id = #{providerId} AND deleted_at IS NULL
    </select>

    <update id="activateUser">
        UPDATE users SET status = 'ACTIVE', updated_at = NOW() WHERE email = #{email} AND deleted_at IS NULL
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (email, password, nickname, role, status, created_at, updated_at)
        VALUES (#{email}, #{password}, #{nickname}, #{role}, 'ACTIVE', NOW(), NOW())
    </insert>

    <select id="findByEmail" resultType="com.cwww.user.domain.User">
        SELECT * FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findByNickname" resultType="com.cwww.user.domain.User">
        SELECT * FROM users WHERE nickname = #{nickname} AND deleted_at IS NULL
    </select>

    <select id="findNicknameById" resultType="string">
        SELECT nickname FROM users WHERE user_id = #{userId} AND deleted_at IS NULL
    </select>

    <select id="findByIds" resultType="com.cwww.user.domain.User">
        SELECT user_id, nickname FROM users
        WHERE user_id IN
        <foreach item="id" collection="userIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted_at IS NULL
    </select>

    <select id="searchByNickname" resultType="com.cwww.user.domain.User">
        SELECT user_id, nickname FROM users
        WHERE nickname LIKE CONCAT(
            REPLACE(REPLACE(REPLACE(#{keyword}, '\', '\\'), '%', '\%'), '_', '\_'), '%'
        ) ESCAPE '\'
          AND deleted_at IS NULL
        ORDER BY nickname
        LIMIT #{limit}
    </select>

    <select id="findById" resultType="com.cwww.user.domain.User">
        SELECT * FROM users WHERE user_id = #{userId} AND deleted_at IS NULL
    </select>

    <update id="saveRefreshToken">
        UPDATE users
        SET refresh_token = #{refreshToken}, refresh_token_expires_at = #{expiresAt}, updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="deleteRefreshToken">
        UPDATE users
        SET refresh_token = NULL, refresh_token_expires_at = NULL, updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updateNickname">
        UPDATE users SET nickname = #{nickname}, updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updateResetToken">
        UPDATE users SET reset_token = #{resetToken}, reset_token_expires_at = #{expiresAt}, updated_at = NOW()
        WHERE email = #{email} AND deleted_at IS NULL
    </update>

    <select id="findByResetToken" resultType="com.cwww.user.domain.User">
        SELECT * FROM users WHERE reset_token = #{resetToken} AND deleted_at IS NULL
    </select>

    <update id="resetPassword">
        UPDATE users SET password = #{password}, reset_token = NULL, reset_token_expires_at = NULL, updated_at = NOW()
        WHERE user_id = #{userId} AND deleted_at IS NULL
    </update>

    <update id="updatePassword">
        UPDATE users SET password = #{password}, updated_at = NOW()
        WHERE user_id = #{userId} AND deleted_at IS NULL
    </update>


    <!-- 회원탈퇴 -->
    <update id="withdraw">
        UPDATE users SET deleted_at = NOW(), updated_at = NOW()
        WHERE user_id = #{userId} AND deleted_at IS NULL
    </update>

</mapper>